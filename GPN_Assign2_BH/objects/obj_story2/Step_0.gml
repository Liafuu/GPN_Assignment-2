a += 0.5;

print = string_copy(str, 1, a);
if ((a > string_length(str)+100) && (next < array_length_1d(strings) - 1)) {
	a = 0;
	next++;
	
	if (next == array_length_1d(strings) - 1) { room_goto(rm_stage2) }
}

str = strings[next];







